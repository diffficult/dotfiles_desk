# ============================================================================
# DAVINCI RESOLVE WINDOW RULES
# ============================================================================

windowrule {
  name = davinci-resolve-dialog-float
  match:class = .*[Rr]esolve.*
  match:float = true

  stay_focused = on
}
